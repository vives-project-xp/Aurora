version: '3'
services:
  website:
    build: ./website
    ports:
      - 80:80
  api:
    build: ./PythonAPI/Flask-MQTT
    ports:
      - 5100:5000
  #  image: python
  #  volumes:
  #   - ./PythonAPI/Flask-MQTT/:/python